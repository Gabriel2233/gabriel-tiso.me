import styled from 'styled-components'

export const Container = styled.header`
  width: 100%;
  padding: 1.25rem;
  backdrop-filter: blur(15px);
  position: sticky;
  top: 0;
  z-index: 10;

  margin-bottom: 1rem;
`
